# ============================================================================
# FORD CAD Configuration - Copy to .env and fill in values
# ============================================================================

# ============================================================================
# SMTP EMAIL SETTINGS (for daily reports and notifications)
# ============================================================================
# Gmail example (use App Password, not regular password):
#   1. Enable 2FA on your Google account
#   2. Go to https://myaccount.google.com/apppasswords
#   3. Generate an App Password for "Mail"
#
# Other SMTP providers:
#   Outlook.com: smtp-mail.outlook.com:587
#   Yahoo: smtp.mail.yahoo.com:587
#   Custom SMTP: your-server.com:587 or :465 for SSL

CAD_SMTP_HOST=smtp.gmail.com
CAD_SMTP_PORT=587
CAD_SMTP_USER=your-email@gmail.com
CAD_SMTP_PASS=your-app-password
CAD_SMTP_FROM=your-email@gmail.com
CAD_SMTP_TLS=true

# ============================================================================
# SIGNAL MESSAGING (requires signal-cli)
# ============================================================================
# Install signal-cli: https://github.com/AsamK/signal-cli
# Register your number first: signal-cli -u +1XXXXXXXXXX register
# Verify: signal-cli -u +1XXXXXXXXXX verify CODE

CAD_SIGNAL_CLI=signal-cli
CAD_SIGNAL_SENDER=+1XXXXXXXXXX

# ============================================================================
# REPORT RECIPIENTS
# ============================================================================
# Battalion Chief contact info for daily reports

CAD_BC_EMAIL=battalion.chief@yourfire.gov
CAD_BC_PHONE=5551234567
CAD_BC_SIGNAL=+15551234567

# ============================================================================
# REPORT SCHEDULER
# ============================================================================
# Set to "true" to auto-send reports 30 min before shift change

CAD_ENABLE_REPORT_SCHEDULER=false

# ============================================================================
# SMS CARRIER GATEWAYS
# ============================================================================
# SMS is sent via email-to-SMS gateways. Supported carriers:
#   att, verizon, tmobile, sprint, uscellular, virgin, boost, cricket, metro, google_fi
#
# To send SMS, use the API:
#   POST /api/message/sms
#   { "phone": "5551234567", "carrier": "verizon", "message": "Your message" }

# ============================================================================
# DATABASE
# ============================================================================
CAD_DB_PATH=cad.db
